import { View, Text } from 'react-native';
import AuthContainer from '../ui/AuthContainer';

const RenderAccount = () => {
return (
        <AuthContainer>
            <View>
                <Text>Esta será a futura tela da conta</Text>
            </View>
        </AuthContainer>
);
}  

export default RenderAccount;